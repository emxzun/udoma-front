import React from 'react';

const SearchField = () => {
  return (
      <div className="search-field">
        <input type="text"/>
        <button>Поиск</button>
      </div>
  );
};

export default SearchField;